"""Plugin framework — the host side of the module system.

Built-in modules and third-party plugins share one contract (``api``),
one manifest format (``manifest``), and one lifecycle owner (``manager``).
"""
