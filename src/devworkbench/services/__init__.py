"""Cross-cutting services shared by modules (no module owns these).

Each service is a plain-Python façade; business logic lands in a later
milestone, so the classes below define the public surface only.
"""
