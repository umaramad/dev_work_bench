# resources/icons/

Branding assets (`.icns` app icon, marketing artwork).

All **runtime** icons are drawn programmatically by
`src/devworkbench/ui/icons.py` (QPainter at 2× devicePixelRatio), so there
are no per-icon PNG/SVG files to maintain.
